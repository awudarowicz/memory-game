# Memory Game for Convious interview

Author: Adam Wudarowicz

## Running game in localhost:

1. clone repository
2. run command `npm install`
3. run command `npm start`
4. visit `http://localhost:3000` in your browser